/**
 * normalized sin, range shifted to [0..1]
 */
export const nsin = (n:number)=>(Math.sin(n)+1)/2

/**
 * normalized sin, range shifted to [0..1]
 */
export const ncos = (n:number)=>(Math.cos(n)+1)/2
